implement get/set size/mode/time with tests

fix extraction with invalid password (SetOperationResult?)
	tests sevenzip-5.10.1 and sevenzip-5.11.1

change syntax to extract:
	handle extract ?-password password? ?-channel? pathOrChannel itemName - extract one item, current implementation
	handle extract ?-password password? ?-directory dir? ?itemsMask? - extract items to joined dir and itemPath?
	handle extract ?-password password? ?destination? - extract all items to destination?

add formats command returns dictionary
	use numeric types as format id for -forcetype

fix create command feature
	password cannot be set on item
	properties cannot be set on item

add create command option -directory:
	sevenzip create ?-properties proplist? ?-forcetype type? ?-password password? ?-directory dir? ?-channel? pathOrChannel filesList

change create command:
	make all items paths relative ?

add directory option to list to limit output:
	handle list -directory dir or -path itempath? 

progress callbacks:
	sevenzip open ?-callback script? ...
	handle extract ?-callback script? ...
	sevenzip create ?-callback script? ...
		where callback is `proc cb {op??? item? progress? total?}`

unload 7z library without fatal `rename sevenzip ""`

internals: streams should be owned by callbacks as members?
